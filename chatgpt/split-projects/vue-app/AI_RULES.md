# AI 修改代码规则

1. 已跑通的业务逻辑不得随意重构。
2. UI 调整默认只允许修改样式，不允许修改业务逻辑。
3. 不得随意删除、重命名、移动已有变量、函数、事件、接口字段。
4. Vue 页面中，v-model、@click、v-if、v-for、props、emit、ref、computed、watch 不能随意改。
5. 修改前必须先理解页面状态、事件、数据流。
6. 修改后必须检查页面原有交互是否仍然可用。
7. 如果用户要求"只改颜色/样式"，不得改布局和逻辑。
8. 如果必须改结构，必须先说明原因。
